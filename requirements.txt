# REQUIREMENTS.TXT – ANTI-ERROR 2025+ (Python 3.9-3.13, Render/Replit/VPS)
# Rangos flexibles: agarra latest estable SIEMPRE, sin pins rotos

# Telegram Bot (latest estable v22.5+ compatible)
python-telegram-bot

# HTTP async
aiohttp

# RAG: Embeddings
sentence-transformers

# Vector search (CPU)
faiss

# Torch (CPU, estable)
torch
numpy

# Opcionales
tqdm
PyYAML
