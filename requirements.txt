# ─────────────────────────────────────────────────────────────
# REQUIREMENTS.TXT – VERSIÓN INDESTRUCTIBLE 2025-2026
# Funciona en Python 3.10, 3.11, 3.12 y 3.13+ (Render, Replit, etc.)
# ─────────────────────────────────────────────────────────────

# Telegram Bot – versión estable más reciente (NO uses versión fija 20.8 que no existe)
python-telegram-bot>=21.0,<22.0

# HTTP async + timeout
aiohttp>=3.9.0

# RAG – Sentence Transformers (ligero y rápido)
sentence-transformers>=2.5.0

# FAISS CPU (índices vectoriales)
faiss-cpu>=1.8.0

# Torch (necesario para sentence-transformers y faiss)
torch>=2.1.0,<2.5.0
torchaudio>=2.1.0,<2.5.0
torchvision>=0.16.0,<0.20.0

# Numpy (siempre actual)
numpy>=1.24.0

# Opcional pero recomendado: tqdm y pyyaml (para sentence-transformers)
tqdm>=4.66.0
PyYAML>=6.0
